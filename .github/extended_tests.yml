# DEPRECATED / INFO
#
# GitHub Actions ne détecte que les workflows situés dans : .github/workflows/
# Ce fichier n'est donc pas exécuté. Le workflow actif est :
#   .github/workflows/extended_tests.yml

deprecated: true
active_workflow: .github/workflows/extended_tests.yml
reason: "GitHub Actions ignore les workflows hors .github/workflows"
