name: delta_stats

on:
  workflow_dispatch:
    inputs:
      run_id:
        description: "Run id"
        required: true
        default: "1"
      seed:
        description: "Seed"
        required: true
        default: "1000"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run delta_stats
        env:
          RUN_ID: ${{ inputs.run_id }}
          SEED: ${{ inputs.seed }}
        run: |
          mkdir -p _ci_out
          python -c "import os; from pathlib import Path; from echonull.delta_stats.analyzer import DeltaStatsAnalyzer; out=Path('_ci_out'); out.mkdir(parents=True, exist_ok=True); print(DeltaStatsAnalyzer().analyze(run_id=int(os.environ['RUN_ID']), seed=int(os.environ['SEED']), data=None, output_dir=out))"

      - name: Upload artefacts
        uses: actions/upload-artifact@v4
        with:
          name: delta_stats_out
          path: _ci_out/delta_stats
