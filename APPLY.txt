EchoNull updates v0.1.2 (consolidated)

Files included:
- .github/workflows/ci.yml
- src/null_trace.py
- .github/extended_tests.yml (info only, not executed by Actions)
- VERSION, README.md, CHANGELOG.md, PATCH_NOTES.md

How to apply:
1) Unzip at the repository root (overwrite files).
2) Commit and push.
3) Check GitHub Actions: CI should pass (lint + tests with coverage >= 100%).

Notes:
- Black exclude regex now matches absolute paths on GitHub runners.
- null_trace shim uses dynamic import and is ruff/mypy clean.
