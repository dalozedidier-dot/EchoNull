# CI metrics

This folder is meant to store append-only, run-by-run summaries.

Suggested layout:

- ci_metrics/raw/ : per-run CSV snapshots (one file per CI run)
- ci_metrics/history.csv : consolidated file generated by scripts/consolidate_history.py

Do not commit any large artifact bundles here.
